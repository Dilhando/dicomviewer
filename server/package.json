{
  "name": "server",
  "version": "1.0.0",
  "description": "A small server to host dicom files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "thzimmer",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  }
}
